<h1><%= @flat.name %></h1>
<p>Address:<%= @flat.address %></p>
<p>Description:<%= @flat.description %></p>
<p>Price per night:<%= @flat.price_per_night %></p>
<p>Number of guests (Up-to):<%= @flat.number_of_guests %></p>

<%= link_to "Back to all flats", flats_path %>
<%= link_to "Edit", edit_flat_path %>
<%= link_to "Delete", flat_path(@flat), method: :delete, data: { confirm: "Are you sure?" } %>
